<%= for piece <- @pieces do %>
  <p>
    <strong><%= person_info(piece) %></strong>
    <br>
    <%= piece.title %>
    <br>
    <span class="text-muted"><%= duration(piece) %> / <%= epoch_info(piece.epoch) %></span>
  </p>
<% end %>
